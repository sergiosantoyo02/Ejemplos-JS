var oracion = "pepe pecas, pica papas; con un pico: con un, pico pepe, pecas; pica papas cuenta cuantos cuentos cuentas por que; cuando. cuentas, cuentos nunca sabes: cuantos. cuentos cuentas";
var oracion2 = "pepe pecas, pica papas; con un pico: con un, pico pepe, pecas; pica papas cuenta cuantos cuentos cuentas por que; cuando. cuentas, cuentos nunca sabes: cuantos. cuentos cuentas";
console.log(mayus(oracion));
console.log(mayusPrimero(oracion2));



function mayus(oracion)
{
    return oracion.toUpperCase();
}   
/*
function firstCapital (oracion2)
{
    return oracion2.replace(/\b\w/g, l => l.toUpperCase());
}*/

function mayusPrimero(oracion2)
{
    for (var i = 0; i<oracion2.length;i++)
        {
            if(oracion2)
            
        }
}
